{
  "name": "marcomms-bot",
  "version": "1.0.0",
  "description": "Slack bot for Pearl marketing intake — handles requests via conversation or form, generates briefs, creates Drive folders, and sets up Monday.com tasks",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "npx tsc -p tsconfig.json && cp src/lib/knowledge-base.md dist/lib/",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@slack/bolt": "^4.1.0",
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.6",
    "dotenv": "^16.4.7",
    "googleapis": "^171.0.0",
    "pg": "^8.13.1",
    "typescript": "^5.7.0"
  },
  "devDependencies": {
    "@slack-wrench/jest-bolt-receiver": "^3.0.0",
    "tsx": "^4.19.0",
    "vitest": "^4.0.18"
  }
}
